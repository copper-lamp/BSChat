# Admin 模块

## 需求
提供可审计的准入控制和玩家音量策略，供服务端会话/混音链路使用。

## 实际状态
`src/server/admin/AdminPolicy` 提供线程安全的白名单、黑名单和 [0,4] 增益；黑名单优先，启用白名单时仅白名单成员允许通过。当前策略是内存对象，尚未由 `ServerRuntime` 装配，也没有命令、持久化和审计日志。

## 风险与 TODO
不能把当前策略当作生产级封禁系统。需要权限校验、持久化迁移、审计记录、重启恢复和与会话创建的集成测试。
